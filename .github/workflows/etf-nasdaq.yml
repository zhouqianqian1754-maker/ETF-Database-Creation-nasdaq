name: ETF Nasdaq Daily Update

on:
  workflow_dispatch:
  schedule:
    - cron: "0 13 * * *"

permissions:
  contents: write

jobs:
  run-etf-script:
    runs-on: ubuntu-latest

    env:
      TZ: Asia/Hong_Kong

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Google Chrome and matching ChromeDriver
        id: setup-chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: 148
          install-chromedriver: true
          install-dependencies: true

      - name: Export browser paths
        run: |
          echo "CHROME_BIN=${{ steps.setup-chrome.outputs.chrome-path }}" >> $GITHUB_ENV
          echo "CHROMEDRIVER_PATH=${{ steps.setup-chrome.outputs.chromedriver-path }}" >> $GITHUB_ENV

      - name: Show browser info
        run: |
          echo "Chrome binary: $CHROME_BIN"
          echo "ChromeDriver path: $CHROMEDRIVER_PATH"
          "$CHROME_BIN" --version
          "$CHROMEDRIVER_PATH" --version

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install -U pandas numpy selenium

      - name: Show workspace before run
        run: |
          pwd
          ls -la
          find . -maxdepth 3 -type d | sort

      - name: Run ETF script
        run: python "ETF nasdaq to github v0.0.py"

      - name: Show generated files
        if: always()
        run: |
          echo "=== data folder ==="
          find data -type f | sort || true
          echo "=== debug folder ==="
          find debug -type f | sort || true
          echo "=== git status ==="
          git status --short

      - name: Upload generated CSV files as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: etf-data
          path: data/

      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: etf-debug
          path: debug/

      - name: Commit updated CSV files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || git commit -m "Daily ETF Nasdaq update"
          git push
